# Screenshots

Real screenshots of the dashboard running against the stored results in `data/results/`.

| File | Shows |
| --- | --- |
| `dashboard-overview.png` | Overview tab: baseline vs. guarded metric cards |
| `category-results.png` | Categories tab: per-category attack success rates |
| `test-explorer.png` | Test explorer with one test expanded (baseline and guarded side by side) |

To retake them, start the API and the dashboard (see the root README), open `http://localhost:5173/#summary`, `#categories` or `#tests`, and capture at about 1200px wide. If the stored results change, recapture so the images match.
